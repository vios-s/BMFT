_target_: src.data.isic_datamodule.ISICDataModule
data_dir: ${paths.skin_dir}
dataset: "isic_balanced"
testset: null
transform: null
test_transform: null
batch_size: 64 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 8
pin_memory: False
